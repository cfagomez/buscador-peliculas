import { useContext } from 'react'
import { BuscadorContext } from '../context/BuscadorProvider'

const useBuscadorContext = () => {
  return (
    
    useContext(BuscadorContext)

  )
}

export default useBuscadorContext
